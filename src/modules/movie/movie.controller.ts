import { getSortedMovies } from "./movie.service";


export async function getMovieList(req: Request, res: Response) {
    // const { page, pageSize, sort } = req.query;
    // const movies = await getSortedMovies(page, pageSize, sort);
    // res.json(movies);
}

export async function getMovieDetail(req: Request, res: Response) {
//     const { id } = req.params;
//     const movie = await getMovieDetail(id);
//     res.json(movie);
}
